FROM python:3.11-slim

WORKDIR /app

RUN pip install --no-cache-dir \
    requests \
    python-owasp-zap-v2.4 \
    elasticsearch>=8.0.0

COPY docker/zap_shipper.py /app/

CMD ["python", "-u", "zap_shipper.py"]
